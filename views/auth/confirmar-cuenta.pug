extends ../layout/index
block contenido
    .py-10
        h1.text-6xl.my-10.font-normal.text-center Maneja
            span.font-extrabold.uppercase.text-green-700 te
        h2.text-center.text-2xl.font-extrabold= pagina

        div(class= "bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto mt-5")
            p(class= error ? "bg-red-500" : "bg-green-500").py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= mensaje

            if !error
                a(href="/auth/login" class= "text-center font-bold texte-sm text-slate-600 uppercase mt-10 block") Ya Puede Iniciar Sesión